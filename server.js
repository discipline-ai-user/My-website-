const http=require('http'),fs=require('fs'),path=require('path');
const ROOT=__dirname,PORT=process.env.PORT||4173,KEY=process.env.GEMINI_API_KEY,MODEL=process.env.GEMINI_MODEL||'gemini-2.5-flash';
const types={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.json':'application/json; charset=utf-8'};
function send(res,status,data,type='application/json'){res.writeHead(status,{'Content-Type':type,'Access-Control-Allow-Origin':'*'});res.end(type.startsWith('application/json')?JSON.stringify(data):data)}
function body(req){return new Promise((ok,bad)=>{let d='';req.on('data',c=>{d+=c});req.on('end',()=>ok(d));req.on('error',bad)})}
const system='You are Discipline AI Tutor for a Class 12 Bihar Board Science student. Help with Physics, Chemistry and Mathematics. Use easy Hinglish when the student writes Hindi/Hinglish. Explain concepts clearly, formulas and simple examples when useful. For MCQs, show options and check answers carefully. Never claim verification you did not do. Keep answers focused on learning.';
async function chat(messages){if(!KEY)throw Object.assign(new Error('GEMINI_API_KEY is not configured on the server.'),{statusCode:503});const contents=[{role:'user',parts:[{text:system}]}];for(const m of Array.isArray(messages)?messages.slice(-12):[]){if((m.role==='user'||m.role==='assistant')&&m.content)contents.push({role:m.role==='assistant'?'model':'user',parts:[{text:String(m.content).slice(0,12000)}]})}const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(MODEL)}:generateContent`,{method:'POST',headers:{'Content-Type':'application/json','x-goog-api-key':KEY},body:JSON.stringify({contents,generationConfig:{temperature:.4,maxOutputTokens:1800}})});const d=await r.json();if(!r.ok)throw Object.assign(new Error(d?.error?.message||'Gemini request failed'),{statusCode:r.status});const reply=(d.candidates||[]).flatMap(c=>c.content?.parts||[]).map(p=>p.text||'').join('').trim();if(!reply)throw Object.assign(new Error('AI returned an empty response.'),{statusCode:502});return reply}
const server=http.createServer(async(req,res)=>{try{if(req.method==='OPTIONS'){res.writeHead(204,{'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'Content-Type'});return res.end()}const u=new URL(req.url,'http://localhost');if(u.pathname==='/api/health')return send(res,200,{ok:true,aiConfigured:Boolean(KEY),model:MODEL});if(u.pathname==='/api/chat'&&req.method==='POST'){let p;try{p=JSON.parse(await body(req))}catch{return send(res,400,{error:'Invalid JSON'})}return send(res,200,{ok:true,reply:await chat(p.messages)})}const requested=u.pathname==='/'?'/index.html':u.pathname;const file=path.normalize(path.join(ROOT,requested));if(!file.startsWith(ROOT))return send(res,403,{error:'Forbidden'});fs.stat(file,(e,st)=>{if(e||!st.isFile())return send(res,404,{error:'Not found'});send(res,200,fs.readFileSync(file),types[path.extname(file)]||'application/octet-stream')})}catch(e){send(res,e.statusCode||500,{error:e.message||'Server error'})}});server.listen(PORT,'0.0.0.0',()=>console.log(`Discipline AI on ${PORT}; Gemini ${KEY?'configured':'not configured'}`));
